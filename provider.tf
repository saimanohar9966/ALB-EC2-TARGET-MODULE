provider "aws" {
  region     = "ap-south-1"                               # Mention the required region
  access_key = ""                     # Mention the access key in place
  secret_key = "" # Mention the secret key in place
}